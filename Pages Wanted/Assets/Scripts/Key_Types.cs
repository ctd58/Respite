// Contains global data about keys, to keep organized
// To add a new key, add a new color to the enum
public enum KeyTypes {
	GOLD,
	IRON,
	BRONZE
};

public class Key_Obj {
	public KeyTypes type;
	public int keyId;
}